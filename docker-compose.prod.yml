version: '3.7'

services:

  aut-dashboard:
    container_name: aut-dashboard
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '5001:80'